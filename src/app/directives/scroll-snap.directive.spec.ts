import { ScrollSnapDirective } from './scroll-snap.directive';

describe('ScrollSnapDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollSnapDirective();
    expect(directive).toBeTruthy();
  });
});
